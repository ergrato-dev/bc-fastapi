[project]
name = "contacts-api"
version = "1.0.0"
description = "API de Gestión de Contactos - Proyecto Semana 02"
requires-python = ">=3.14"
dependencies = [
    "fastapi==0.128.0",
    "pydantic[email]==2.10.0",
    "uvicorn==0.40.0",
]

[tool.uv]
dev-dependencies = [
    "pytest==8.4.0",
    "httpx==0.29.0",
]
